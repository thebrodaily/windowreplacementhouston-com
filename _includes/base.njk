<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- 
    ======================================================================
    SEO METADATA - This is now dynamic.
    The title and description will be pulled from the front matter of each page.
    ======================================================================
    -->
    <title>{{ title }}</title>
    <meta name="description" content="{{ description }}">

    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    
    <!-- Google Fonts: Inter -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;900&display=swap" rel="stylesheet">

    <!-- Alpine.js for Dropdowns & FAQ -->
    <script src="https://cdn.jsdelivr.net/npm/@alpinejs/focus@3.x.x/dist/cdn.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>

    <style>
        body {
            font-family: 'Inter', sans-serif;
            color: #334155; /* slate-700 */
        }
        .cta-button {
            transition: all 0.3s ease;
        }
        .cta-button:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 20px rgba(0,0,0,0.1);
        }
        .hero-bg {
            background-image: url('https://placehold.co/1920x1080/e0e7ff/374151?text=Modern+Houston+Home');
            background-size: cover;
            background-position: center;
        }
        .area-card {
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }
        .area-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 30px rgba(0,0,0,0.1);
        }
        .dropdown-menu {
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }
    </style>

</head>
<body class="bg-white antialiased">

    <!-- 
      This is where the content from your individual pages (like index.html) 
      will be automatically injected by the Static Site Generator.
    -->
    {{ content | safe }}

</body>
</html>
